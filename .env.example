# Porta do servidor (padrão: 5000)
# Para VPS com outro sistema na porta 5000, use 5001, 5002, etc.
PORT=5000

# Ambiente
NODE_ENV=production

# Chave secreta para sessões (gere uma string aleatória longa)
# Exemplo: openssl rand -hex 32
SESSION_SECRET=mude_para_uma_chave_secreta_aleatoria_longa
